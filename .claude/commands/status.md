---
description: One-screen read-only status — branch, last commit, uncommitted count, stashes, open PRs, next action.
---

One-screen status: current branch + last commit, uncommitted file count, stash count, open PRs (`gh pr list`), and the recommended next action from `docs/tickets.md`. Read-only.
