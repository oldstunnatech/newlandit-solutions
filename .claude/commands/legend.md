---
description: List every project slash command grouped by area, one line each. Read-only.
---

List every project command below with a one-line description, grouped:
**Git & branching** (`/start`, `/commit`, `/sync`, `/cleanup`, `/promote`, `/ship`),
**Tickets & docs** (`/tickets`, `/new-ticket`, `/advice`, `/docs-audit`),
**Quality** (`/check`, `/security-check`),
**Info** (`/status`, `/paste-error`, `/legend`).
Read-only.
