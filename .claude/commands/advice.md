---
description: Compact next-action recommendation from current branch + docs/tickets.md state. Read-only, executes nothing.
---

Compact recommendation when the user asks "what do you advise?". Read the current branch + `docs/tickets.md` state. Output one paragraph (max 2 sentences) with the recommended next ticket/action + a single-line tradeoff. If multiple paths: `(a)`, `(b)`, `(c)` one-liners, mark the recommended one. Do NOT execute anything.
