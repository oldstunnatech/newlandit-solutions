---
description: Merge latest development into the current working branch. Stops on uncommitted changes or conflicts.
---

Merge latest `development` into the current working branch. STOP if uncommitted changes exist or if already on `main`/`development`. On conflicts: list the files, stop, let the user resolve. For `docs/tickets.md` conflicts: keep both ticket entries, preserve table ordering, remove all conflict markers.
